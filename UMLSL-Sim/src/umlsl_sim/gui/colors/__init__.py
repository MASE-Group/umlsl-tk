"""Colours that only exist because something is drawn: roads, lanes, wrecks.

Car colours are *not* here -- a car's colour is part of its identity, so it
lives in `umlsl_sim.palettes` where the car factory can reach it without
importing the GUI.
"""
